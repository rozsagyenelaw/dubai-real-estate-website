# Email Configuration for Netlify Functions
# Copy this file to .env and fill in your actual values

# SMTP Email Settings (Required)
# Use Gmail, SendGrid, Mailgun, or any SMTP service

# For Gmail (easiest to start):
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Where to send form submissions
CONTACT_EMAIL=info@premierdubairealty.com

# === SETUP INSTRUCTIONS ===

# Gmail Setup (Recommended for Testing):
# 1. Go to https://myaccount.google.com/apppasswords
# 2. Create an "App Password" for "Mail"
# 3. Use that password (not your regular Gmail password) above
# 4. Set SMTP_USER to your Gmail address

# SendGrid Setup (Recommended for Production):
# 1. Sign up at https://sendgrid.com (Free: 100 emails/day)
# 2. Create an API key
# 3. Use these settings:
#    SMTP_HOST=smtp.sendgrid.net
#    SMTP_PORT=587
#    SMTP_USER=apikey
#    SMTP_PASS=your-sendgrid-api-key

# Mailgun Setup (Alternative):
# 1. Sign up at https://mailgun.com
# 2. Get your SMTP credentials
# 3. Use your Mailgun SMTP settings

# IMPORTANT: Never commit .env file to git!
# It's already in .gitignore
